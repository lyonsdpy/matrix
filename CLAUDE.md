# CLAUDE.md

项目行为准则，合并了 Karpathy 编码指南与本项目特定规范。

**权衡说明：** 这些准则偏向谨慎而非速度。对于琐碎任务，自行判断即可。

---

## 1. 编码前思考

**不要假设。不要隐藏困惑。呈现权衡。**

实现前：
- 明确说明你的假设。如果不确定，先问。
- 如果存在多种解释，列出来 —— 不要默默选择。
- 如果存在更简单的方案，说出来。必要时提出异议。
- 如果有不清楚的地方，停下来，指出困惑所在，要求澄清。

## 2. 简洁优先

**用最少的代码解决问题。不要过度推测。**

- 不添加需求之外的功能。
- 不为一次性代码创建抽象。
- 不添加未要求的"灵活性"或"可配置性"。
- 不为不可能发生的场景做错误处理。
- 如果 200 行能写成 50 行，就重写。

检验标准：资深工程师会觉得这过于复杂吗？如果是，简化。

## 3. 精准修改

**只碰必须碰的。只清理自己造成的混乱。**

编辑现有代码时：
- 不要"改进"相邻的代码、注释或格式。
- 不要重构没坏的东西。
- 匹配现有风格，即使你更倾向于不同的写法。
- 如果注意到无关的死代码，提一下 —— 不要删除它。

当你的改动产生孤儿代码时：
- 删除因你的改动而变得无用的导入/变量/函数。
- 不要删除预先存在的死代码，除非被要求。

检验标准：每一行修改都应该能直接追溯到用户的请求。

## 4. 目标驱动执行

**定义成功标准。循环验证直到达成。**

将任务转化为可验证的目标：
- "添加验证" → "为无效输入编写测试，然后让它们通过"
- "修复 bug" → "编写重现 bug 的测试，然后让它通过"
- "重构 X" → "确保重构前后测试都能通过"

对于多步骤任务，说明一个简短的计划：
```
1. [步骤] → 验证: [检查]
2. [步骤] → 验证: [检查]
3. [步骤] → 验证: [检查]
```

强有力的成功标准让执行可以独立循环。弱标准（"让它工作"）需要不断澄清。

---

**判断准则是否生效：** diff 中不必要的改动更少、因过度复杂导致的重写更少、澄清问题在实现之前提出而非犯错之后。

---

## 项目特定规范

- 语言：所有回复必须使用中文
- 架构：Go+Gin 三层架构（repository → service → handler），新增功能严格遵循此分层
- 注释：关键逻辑位置加中文注释，聚焦"为什么"而非"是什么"
- 构建：每次修改后 `go build` 必须通过，所有调用方必须同步更新
- 风格：同包内有已有实现时必须对齐，不擅自引入不同风格
